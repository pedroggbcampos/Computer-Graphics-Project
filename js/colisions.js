/**
 * COLISION DETECTION FUNCTIONS
 **/

function objs_colision_detection(obj_array) {
  for (var i=0; i < obj_array.length; i++) {
    for (var j=i+1; j < obj_array.length; j++) {

      //detect_colision(obj_array[i], obj_array[j]) {

      //}
    }
  }
}
